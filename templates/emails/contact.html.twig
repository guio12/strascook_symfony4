<!DOCTYPE html>
<html>
<body>

<h2>C'est moche mais osef</h2>

<table style="width:100%">
  <tr>
    <th>Prénom</th>
    <th>Nom</th> 
    <th>Email</th>
    <th>Téléphone</th>
    <th>Message</th>
  </tr>
  <tr>
    <td>{{ contact.firstname}}</td>
    <td>{{ contact.lastname}}</td>
    <td>{{ contact.email}}</td>
    <td>{{ contact.phone}}</td>
    <td>{{ contact.message}}</td>
  </tr>
</table>

</body>
</html>
