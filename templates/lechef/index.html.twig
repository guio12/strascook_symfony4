{% extends "base.html.twig" %}

{% block body %}

{% for item in items %}
{# Style dégueu pour test fonctionnel du back #}
<div class="card" style="width: 18rem;">
        <img src="{{ asset('build/images/uploads/lechef/') }}{{ item.image }}" class="card-img-top" alt="...">
        <div class="card-body">
          <h2 class="card-text">{{ item.titre }}</h2>
          <p class="card-text">{{ item.description }}</p>
        </div>
      </div>
{% endfor %}

{% endblock body %}